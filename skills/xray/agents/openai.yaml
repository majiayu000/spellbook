interface:
  display_name: "X-Ray Explainer"
  short_description: "See the big picture, then inspect the evidence"
  default_prompt: "Use $xray to investigate how this actually works, then explain it in two depths: first a dead-simple big-picture visual, followed by source-backed code, network, runtime, or safe static artifact evidence for readers who want to dig deeper."
