interface:
  display_name: "Threads"
  short_description: "Coordinate native Codex threads with evidence gates and run logs"
  default_prompt: "Use $threads to plan and run a Codex thread workflow with intent contracts, worktrees, reviews, merge gates, review-thread closure, and run-log data collection."
